"""App configuration for Ultimate PDF application."""

from django.apps import AppConfig


class UltimatePDFConfig(AppConfig):
    """Configuration for the Ultimate PDF application."""
    
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'app'